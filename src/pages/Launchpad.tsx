import { Layout } from "@/components/Layout";
import { ICOLaunchpad } from "@/components/ICOLaunchpad";

const Launchpad = () => {
  return (
    <Layout>
      <ICOLaunchpad />
    </Layout>
  );
};

export default Launchpad;